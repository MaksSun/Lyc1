# Frontend (React + Vite)

## Запуск
```bash
cd frontend
npm install
npm run dev
```

Откройте:
- http://localhost:5173

Требуется запущенный backend на:
- http://localhost:8000

npm run dev -- --host
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
